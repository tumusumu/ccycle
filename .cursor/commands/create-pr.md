# Open a PR

- Must be on non-main/prod branch (bail if not)
- Check diff, commit unstaged work if needed (use `gh`)
- PR format:
```
<area>: <Title> (≤80 chars)
<TLDR> (≤2 sentences)
- 1-3 bullet points
```
- Use `GIT_EDITOR=true` for all git commands
- Return clickable PR link